# Camera-to-Arm Workspace Configuration
#
# Coordinate mapping:
#   - Camera Y (horizontal, 0→1) → Arm Z (left-right)
#   - Camera Z (vertical, 0→1)   → Arm X (front-back, inverted)
#   - Arm Y (height)             → Dynamically computed for safety

# Workspace bounds in meters
workspace:
  # Arm X axis (forward/backward from base)
  x_arm:
    min: 0.10   # closest to base
    max: 0.35   # furthest from base

  # Arm Z axis (left/right)
  z_arm:
    min: -0.15  # right side (from arm's perspective)
    max: 0.15   # left side

  # Arm Y axis (height) - used for clamping
  y_arm:
    min: 0.05   # minimum safe height
    max: 0.25   # maximum height

# Safe plane projection strategy
safe_plane:
  # Strategy: "constant" or "adaptive"
  strategy: "adaptive"

  # Base Y height (used for constant strategy, base for adaptive)
  base_y: 0.10

  # Adaptive strategy parameters
  # Y increases as end-effector moves further from workspace center
  distance_factor: 0.05   # Y increase per meter from center
  y_min: 0.05             # minimum Y value
  y_max: 0.25             # maximum Y value

# End-effector orientation (degrees)
# Default: pointing downward for top-down grasping
orientation:
  roll: 0.0
  pitch: 90.0   # 90° = pointing straight down
  yaw: 0.0

# Motion control settings
motion:
  # Default speed factor (0.0 to 1.0)
  default_speed: 0.3

  # Singularity avoidance
  singularity_check: true
  singularity_threshold: 0.001
